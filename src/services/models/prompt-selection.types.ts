export interface characterSchema {
  id: string;
  name: string;
  description: string;
}

export interface settingSchema {
  id: string;
  name: string;
  description: string;
}

export interface promptsSchema {
  id: string;
  name: string;
  description: string;
}