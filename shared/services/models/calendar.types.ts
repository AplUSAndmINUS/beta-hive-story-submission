export interface calendarSchema {
  name: string;
  date: string;
  description: string;
}
export interface calendarEventSchema {
  id: string;
  name: string;
  date: string;
  description: string;
}